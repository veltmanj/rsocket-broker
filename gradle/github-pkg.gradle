subprojects {

  plugins.withType(MavenPublishPlugin) {
	publishing {
	  repositories {
		maven {
		  def githubRepositoryOwner = System.getenv("GITHUB_REPOSITORY_OWNER") ?: "rsocket-broker"
		  name = "GitHubPackages"
		  url = uri("https://maven.pkg.github.com/${githubRepositoryOwner}/rsocket-broker")
		  credentials {
			username = project.findProperty("gpr.user") ?: githubRepositoryOwner
			password = project.findProperty("gpr.key") ?: System.getenv("GITHUB_TOKEN")
		  }
		}
	  }
	}

	tasks.named("publish").configure {
	  onlyIf { System.getenv('SKIP_RELEASE') != "true" }
	}
  }
}